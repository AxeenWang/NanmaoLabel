<UserControl x:Class="NanmaoLabelPOC.Views.LabelPrintView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:NanmaoLabelPOC.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1200"
             Background="#F5F5F5">

    <!--
    標籤列印分頁
    [ref: raw_spec 8.4]

    佈局：
    - 左側 45%：預覽區 + 格式選擇
    - 右側 55%：資料清單 + 操作按鈕
    -->

    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <!-- 左側：預覽區 45* [ref: raw_spec 8.4] -->
            <ColumnDefinition Width="45*"/>
            <!-- 右側：資料清單 55* [ref: raw_spec 8.4] -->
            <ColumnDefinition Width="55*"/>
        </Grid.ColumnDefinitions>

        <!-- 左側區域 -->
        <Grid Grid.Column="0" Margin="0,0,8,0">
            <Grid.RowDefinitions>
                <!-- 預覽區 1* -->
                <RowDefinition Height="*"/>
                <!-- 格式選擇 70px [ref: raw_spec 8.4] -->
                <RowDefinition Height="70"/>
            </Grid.RowDefinitions>

            <!-- 標籤預覽區 [ref: raw_spec 8.4] -->
            <Border Grid.Row="0"
                    Background="#F5F5F5"
                    BorderBrush="#E0E0E0"
                    BorderThickness="1"
                    CornerRadius="4">
                <Grid>
                    <!-- 預覽內容（Canvas 用於渲染指令）-->
                    <Viewbox Stretch="Uniform"
                             Margin="16"
                             Visibility="{Binding HasSelectedRecord, Converter={StaticResource BoolToVisibilityConverter}}">
                        <!-- 標籤背景：依模板尺寸動態調整 [ref: Delta Spec FR-001, raw_spec 8.4] -->
                        <!-- QW075551-1: 100mm × 80mm → 400px × 320px -->
                        <!-- QW075551-2: 100mm × 60mm → 400px × 240px -->
                        <Border Width="{Binding PreviewWidth}" Height="{Binding PreviewHeight}"
                                Background="White"
                                BorderBrush="#CCCCCC"
                                BorderThickness="1">
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="2" Opacity="0.3" BlurRadius="8"/>
                            </Border.Effect>
                            <!-- 預覽 Canvas：渲染標籤內容 -->
                            <Canvas x:Name="PreviewCanvas"
                                    Width="{Binding PreviewWidth}" Height="{Binding PreviewHeight}"
                                    ClipToBounds="True"/>
                        </Border>
                    </Viewbox>

                    <!-- 空白狀態提示 [ref: raw_spec 8.4] -->
                    <StackPanel HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Visibility="{Binding HasSelectedRecord, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                        <TextBlock Text="📋"
                                   FontSize="48"
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,16"/>
                        <TextBlock Text="請先匯入資料"
                                   FontSize="16"
                                   Foreground="#888888"
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="或選取資料項目"
                                   FontSize="16"
                                   Foreground="#888888"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- 格式選擇區 [ref: raw_spec 8.6] -->
            <!-- T061: 下拉選單高度 36-40px -->
            <Grid Grid.Row="1" Margin="0,16,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="標籤格式"
                           VerticalAlignment="Center"
                           FontSize="14"
                           FontWeight="Bold"
                           FontFamily="Microsoft JhengHei"
                           Margin="0,0,12,0"/>

                <!-- 格式下拉選單 [ref: raw_spec 8.4, 8.6] -->
                <!-- T061: 下拉選單 min 36px, recommended 40px -->
                <ComboBox Grid.Column="1"
                          ItemsSource="{Binding AvailableTemplates}"
                          SelectedItem="{Binding SelectedTemplate}"
                          DisplayMemberPath="DisplayName"
                          MinHeight="40"
                          Height="40"
                          FontSize="14"
                          FontFamily="Microsoft JhengHei"
                          Padding="8,0"/>
            </Grid>
        </Grid>

        <!-- 右側區域 -->
        <Grid Grid.Column="1" Margin="8,0,0,0">
            <Grid.RowDefinitions>
                <!-- 資料清單 1* -->
                <RowDefinition Height="*"/>
                <!-- 分頁指示器 Auto -->
                <RowDefinition Height="Auto"/>
                <!-- 操作按鈕 70px [ref: raw_spec 8.4] -->
                <RowDefinition Height="70"/>
            </Grid.RowDefinitions>

            <!-- 資料清單 ListView [ref: raw_spec 8.4] -->
            <Border Grid.Row="0"
                    Background="White"
                    BorderBrush="#E0E0E0"
                    BorderThickness="1"
                    CornerRadius="4">
                <Grid>
                    <!-- 有資料時顯示 ListView [ref: raw_spec 8.4] T070 -->
                    <ListView x:Name="RecordListView"
                              ItemsSource="{Binding PagedRecords}"
                              SelectedItem="{Binding SelectedRecord, Mode=TwoWay}"
                              SelectionMode="Single"
                              BorderThickness="0"
                              Background="Transparent"
                              Visibility="{Binding HasRecords, Converter={StaticResource BoolToVisibilityConverter}}"
                              MouseDoubleClick="RecordListView_MouseDoubleClick">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <!-- 項目高度 50px [ref: raw_spec 8.4, 8.6] -->
                                <!-- T061: ListView 項目 min 40px, recommended 50px -->
                                <Grid x:Name="ItemGrid" Height="50" MinHeight="50" Margin="8,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- 單據編號 [ref: raw_spec 8.4, 8.6] -->
                                    <!-- T025: 清單項目字體 16pt [US4, FR-005], Selected 時 Bold + White [US1, FR-011] -->
                                    <StackPanel Grid.Column="0"
                                                Orientation="Horizontal"
                                                VerticalAlignment="Center">
                                        <TextBlock x:Name="IconText" Text="📦 " FontSize="16"/>
                                        <TextBlock x:Name="Ogb19Text"
                                                   Text="{Binding Ogb19}"
                                                   FontSize="16"
                                                   FontWeight="Bold"
                                                   FontFamily="Microsoft JhengHei"/>
                                    </StackPanel>

                                    <!-- 分隔線 + 客戶名稱 -->
                                    <StackPanel Grid.Column="1"
                                                Orientation="Horizontal"
                                                VerticalAlignment="Center"
                                                Margin="16,0">
                                        <TextBlock x:Name="Sep1Text"
                                                   Text="│ "
                                                   Foreground="#CCCCCC"
                                                   FontSize="16"/>
                                        <TextBlock x:Name="NvrCustText"
                                                   Text="{Binding NvrCust}"
                                                   FontSize="16"
                                                   FontFamily="Microsoft JhengHei"
                                                   TextTrimming="CharacterEllipsis"
                                                   MaxWidth="200"/>
                                    </StackPanel>

                                    <!-- 數量 -->
                                    <StackPanel Grid.Column="2"
                                                Orientation="Horizontal"
                                                VerticalAlignment="Center">
                                        <TextBlock x:Name="Sep2Text"
                                                   Text="│ "
                                                   Foreground="#CCCCCC"
                                                   FontSize="16"/>
                                        <TextBlock x:Name="Ogd09Text"
                                                   Text="{Binding Ogd09, StringFormat=N0}"
                                                   FontSize="16"
                                                   FontFamily="Microsoft JhengHei"/>
                                    </StackPanel>
                                </Grid>
                                <!-- T014: DataTrigger 處理 Selected 狀態字體樣式 [US1, FR-011] -->
                                <DataTemplate.Triggers>
                                    <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListViewItem}}" Value="True">
                                        <Setter TargetName="Ogb19Text" Property="Foreground" Value="White"/>
                                        <Setter TargetName="NvrCustText" Property="Foreground" Value="White"/>
                                        <Setter TargetName="NvrCustText" Property="FontWeight" Value="Bold"/>
                                        <Setter TargetName="Ogd09Text" Property="Foreground" Value="White"/>
                                        <Setter TargetName="Ogd09Text" Property="FontWeight" Value="Bold"/>
                                        <Setter TargetName="Sep1Text" Property="Foreground" Value="White"/>
                                        <Setter TargetName="Sep2Text" Property="Foreground" Value="White"/>
                                    </DataTrigger>
                                </DataTemplate.Triggers>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                        <!-- T012/T013: 套用共用 ListViewItemStyle [US1] -->
                        <ListView.ItemContainerStyle>
                            <StaticResource ResourceKey="ListViewItemStyle"/>
                        </ListView.ItemContainerStyle>
                    </ListView>

                    <!-- 無資料時顯示空白狀態 [ref: raw_spec 8.4] -->
                    <StackPanel HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Visibility="{Binding HasRecords, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                        <TextBlock Text="📂"
                                   FontSize="48"
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,16"/>
                        <TextBlock Text="尚無資料"
                                   FontSize="18"
                                   Foreground="#888888"
                                   HorizontalAlignment="Center"
                                   FontFamily="Microsoft JhengHei"/>
                        <TextBlock Text="請至「資料管理」分頁匯入 Excel"
                                   FontSize="14"
                                   Foreground="#AAAAAA"
                                   HorizontalAlignment="Center"
                                   FontFamily="Microsoft JhengHei"
                                   Margin="0,8,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- ============================================================ -->
            <!-- 分頁導航區（置中）[FR-006]                                    -->
            <!-- T014: 分頁導航按鈕（◀ ▶）置中顯示                            -->
            <!-- T017: 套用 SecondaryButtonStyle                               -->
            <!-- ============================================================ -->
            <StackPanel Grid.Row="1"
                        Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        Margin="0,8"
                        Visibility="{Binding HasRecords, Converter={StaticResource BoolToVisibilityConverter}}">
                <!-- 上一頁按鈕 ◀ -->
                <Button Command="{Binding PreviousPageCommand}"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Width="50" Height="50"
                        FontSize="18"
                        FontFamily="Microsoft JhengHei"
                        Content="◀"
                        ToolTip="上一頁"/>

                <!-- 頁碼指示器 -->
                <TextBlock Text="{Binding PageIndicatorText}"
                           VerticalAlignment="Center"
                           FontSize="14"
                           FontWeight="Bold"
                           FontFamily="Microsoft JhengHei"
                           Foreground="#333333"
                           Margin="16,0"/>

                <!-- 下一頁按鈕 ▶ -->
                <Button Command="{Binding NextPageCommand}"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Width="50" Height="50"
                        FontSize="18"
                        FontFamily="Microsoft JhengHei"
                        Content="▶"
                        ToolTip="下一頁"/>
            </StackPanel>

            <!-- ============================================================ -->
            <!-- 操作按鈕區（並排置中）[FR-005]                                -->
            <!-- T013: 輸出 PDF 和批次輸出全部按鈕並排置中顯示                 -->
            <!-- T016: 套用 ActionButtonStyle                                  -->
            <!-- ============================================================ -->
            <StackPanel Grid.Row="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        Margin="0,8,0,0">
                <!-- 輸出 PDF 按鈕 -->
                <Button Command="{Binding ExportPdfCommand}"
                        Style="{StaticResource ActionButtonStyle}"
                        MinWidth="140" MinHeight="50"
                        Height="50"
                        FontSize="14"
                        FontFamily="Microsoft JhengHei">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📄 " FontSize="18" VerticalAlignment="Center"/>
                        <TextBlock Text="輸出 PDF" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- 批次輸出全部按鈕（間距 16px）-->
                <Button x:Name="BatchExportButton"
                        Command="{Binding BatchExportCommand}"
                        Style="{StaticResource ActionButtonStyle}"
                        MinWidth="140" MinHeight="50"
                        Height="50"
                        Margin="16,0,0,0"
                        FontSize="14"
                        FontFamily="Microsoft JhengHei"
                        Click="BatchExportButton_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📑 " FontSize="18" VerticalAlignment="Center"/>
                        <TextBlock Text="批次輸出全部" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
